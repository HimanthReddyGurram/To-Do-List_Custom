<%- include("header") -%>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Users Dropdown
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% tot.forEach(element => {%>
              <% if(element.name == "Default list") { %>
                <li><a class="dropdown-item" href="/"><%=  element.name  %></a></li>
              <% }else{ %>
            <li><a class="dropdown-item" href="<%=  element.name  %>"><%=  element.name  %></a></li>
            <% }}) %>
          </ul>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about/a" tabindex="-1" aria-disabled="true">About</a>
          </li>
      </ul>
      <form class="item" method="post" action="/a">
        <input class="form-control me-2" name="name" type="search" placeholder="Add new user" aria-label="Search">
        <button class="btn btn-outline-success" type="submit" >Add</button>
      </form>
    </div>
  </div>
</nav>


  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">
    <% newListItems.forEach(element => {%>
      <form action="/delete" method="post">
      <div class="item">
        <input type="checkbox" name="delvar" value="<%= element.id %>" onChange="this.form.submit()">
        <p><%=  element.name  %></p>
      </div>
    <input type="hidden" name="listName" value="<%= listTitle %>"></input>
    </form>
      <% }) %>


      
      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button color="green" type="submit" name="list" value="<%= listTitle %>">+</button>
      </form>
  </div>

<%- include("footer") -%>
